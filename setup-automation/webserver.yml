---
- hosts: webserver
  become: true
  gather_facts: false
  roles:
    - git-repo

  tasks:
    - name: pull node image
      docker_image:
        name: node
        state: present
        tag: 'argon'
